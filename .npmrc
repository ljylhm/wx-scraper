# 设置Node.js版本要求
engine-strict=true
engines.node=">=18.0.0"

# 使用淘宝镜像源加速包下载
registry=https://registry.npmmirror.com/

# 禁止自动安装可选依赖，减少安装时间
ignore-optional=true

# 设置save-exact=true，保证依赖版本的一致性
save-exact=true

# 设置进度条显示
progress=true 